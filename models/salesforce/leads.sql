SELECT
    Id as lead_id, 
    ActionCadenceAssigneeId as action_cadence_assigneeid,
    ActionCadenceId as action_cadence_id,
    ActivityMetricId as activity_metric_id,
    Address as address,
    AnnualRevenue as annual_revenue,
    City as city,
    CleanStatus as clean_status,
    Company as company,
    CompanyDunsNumber as company_duns_number,
    ConnectionReceivedId as connection_received_id,
    ConnectionSentId as connection_sent_id,
    ConvertedAccountId as converted_account_id,
    ConvertedContactId as converted_contact_id,
    ConvertedDate as converted_dat,
    ConvertedOpportunityId as converted_opportunity_id,
    Country as country,
    CountryCode as country_code,
    CurrencyIsoCode as currency_iso_code,
    Description as description,
    Division as division,
    Email as email,
    EmailBouncedDate as email_bounced_at,
    EmailBouncedReason as email_bounced_reason,
    Fax as fax,
    FirstCallDateTime as first_call_at,
    FirstEmailDateTime as first_email_at,
    FirstName as first_name,
    HasOptedOutOfEmail as is_opted_out_of_email,
    HasOptedOutOfFax as is_opted_out_of_fax,
    GeocodeAccuracy as geocode_accuracy,
    IndividualId as individual_id,
    Industry as industry,
    IsConverted as is_converted,
    IsDeleted as is_deleted,
    IsUnreadByOwner as is_unread_by_owner,
    Jigsaw as jigsaw,
    LastActivityDate as last_activity_at,
    LastName as last_name,
    LastReferencedDate as last_referenced_at,
    LastViewedDate as last_viewed_at,
    Latitude as latitude,
    Longitude as longitude,
    LeadSource as lead_source,
    MasterRecordId as master_record_id,
    MiddleName as middle_name,
    MobilePhone as mobile_phone,
    Name as name,
    NumberOfEmployees as number_of_employees,
    OwnerId as owner_id,
    PartnerAccountId as partner_account_id,
    Phone as phone,
    PhotoUrl as photo_url,
    PostalCode as postal_code,
    Rating as rating,
    RecordTypeId as record_type_id,
    Salutation as salutation,
    ScoreIntelligenceId as score_intelligence_id,
    State as state,
    StateCode as statecode,
    Status as status,
    Street as street,
    Suffix as suffix,
    Title as title,
    Website as website,
    CreatedById as created_by_id,
    CreatedDate as created_at,
    LastModifiedById as last_modified_by_id,
    LastModifiedDate as last_modified_at,
    SystemModstamp as system_modified_at
FROM
    {{ source('salesforce_data', 'lead') }}